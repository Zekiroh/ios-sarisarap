import SwiftUI

// Custom color set used across the entire app
extension Color {
    static let sariPrimary = Color("ssPrimary")
    static let sariSecondary = Color("ssSecondary")
    static let sariBackground = Color("ssBackground")
    static let sariCard = Color("ssCard")
    static let sariText = Color("ssText")
}
